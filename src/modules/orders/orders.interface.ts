export enum CartTypeEnum {
  Photo_Print = 1,
}

export enum OrderStatusEnum {
  Pending = 1,
  Delivered = 2,
  Canceled = 3,
  InCart = 4,
}

export enum paymentStatusEnum {
  success = 1,
  failed = 2,
  Pending = 3,
}
